declare module '@env' {
  // Declare the exact variables you have in your .env file
  // Use `const` declarations for static values
  export const API_BASE_URL: string;
  export const MAPS_API_KEY: string;
}